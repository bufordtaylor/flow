Raw transcripts with the output the rules cleaner must produce (default style unless noted).

- `sample.txt` → `sample.expected.txt` (tone neutral, format plain)
- `dictionary.txt` → `dictionary.expected.txt` (with `Wolchonok | wall chunk` in the dictionary)
- `list.txt` → `list.expected.md` (format markdown; with format plain it stays one sentence)
